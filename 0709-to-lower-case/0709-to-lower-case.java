class Solution {
    public String toLowerCase(String s) {

        // String res = "";

        // for (int i = 0; i < s.length(); i++) {

        //     char ch = s.charAt(i);
        //     int asci = (int) ch;

        //     // already lowercase
        //     if (asci >= 97 && asci <= 122) {
        //         res += ch;
        //     }

        //     // uppercase
        //     else if (asci >= 65 && asci <= 90) {
        //         char cha = (char) (asci + 32);
        //         res += cha;
        //     }

        //     // numbers, spaces, symbols
        //     else {
        //         res += ch;
        //     }
        // }

        // return res;

        // ORR

        return s.toLowerCase();
    }
}